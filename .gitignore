# Homebrew
Brewfile.lock.json

# macOS
.DS_Store
*.swp
*.swo

# Local overrides
*.local
.zshrc.local

# Secrets (never commit)
.env
.envrc.local
*.pem
*.key

# Neovim
# (lazy-lock.json is tracked for reproducibility)
